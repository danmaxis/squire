# Segredos de interpolação do docker-compose.yml da stack.
# Copie para deploy/.env (gitignored) e preencha.
#
# Token de escrita do dashboard (mesmo valor que está hoje em
# squire-dashboard/.env). Sem ele, as rotas POST respondem 503.
DASHBOARD_WRITE_TOKEN=
